{
  "name": "my-studio-ghibli",
  "version": "0.1.0",
  "private": true,
  "homepage": "./",
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.4.0",
    "@testing-library/user-event": "^7.2.1",
    "node-sass": "^7.0.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-loading": "^2.0.3",
    "react-loadingg": "^1.7.1",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.3.0",
    "serve": "^11.3.0"
  },
  "scripts": {
    "start": "serve -s build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "docs": "rm -rf docs && react-scripts build && mv build docs",
    "push-docs": "rm -rf docs && react-scripts build && mv build docs && git add docs/* && git commit -m \"Update docs\" && git push",
    "deploy": "serve -s build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
